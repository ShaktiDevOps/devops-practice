pipeline {
    agent any

    tools {
        maven 'Maven-3' // Must match the Name configured in Jenkins Tools UI
    }

    stages {
        stage('Checkout') {
            steps {
                git branch: 'master', url: 'https://github.com/ShaktiDevOps/devops-practice.git'
            }
        }

        stage('Build') {
            steps {
                dir('a2345') {
                    sh 'mvn clean package'
                }
            }
        }

        stage('Docker Build') {
            steps {
                dir('a2345') {
                    sh 'docker build -t demo-app:v1 .'
                }
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}
